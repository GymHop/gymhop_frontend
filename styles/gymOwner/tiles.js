import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  textContainer: {
    display: 'flex',
    alignItems: "center",
    justifyContent: "space-around"
  },
    headlineNumber: {
      fontSize: 25
    },
    descriptor: {
      fontSize: 15
    }
})
